---
title: "Page Not Found"
excerpt: "Page not found. Your pixels are in another canvas."
sitemap: false
permalink: /404.html
---

<div id="error-message">
Loading error message...
</div>

<script>
  const messages = [
    "404: This page has left the building 🚀\nIt’s probably debugging somewhere in another dimension of the web… or maybe it just didn’t compile.\nTry going back to the homepage or exploring other projects—you know, <code>git push origin main</code> your way back.",
    "404: Oops! This page went missing like a branch with no commit.\nMaybe try checking out the homepage, or <code>git log</code> for your navigation history.",
    "404: Page Not Found\nHave you tried turning it off and on again?\nOr just head back to the homepage before this error gets upvoted to oblivion.",
    "404: Segmentation Fault — the page does not exist.\nIt tried to access memory that wasn’t allocated.\nReturn to the homepage before the stack overflows.",
    "404: “This is fine.” 🔥\nThe page is on fire… or maybe it never existed.\nEscape back to the homepage before things get worse.",
    "404: NullPointerException — looks like this page doesn’t exist.\nMaybe <code>rm -rf</code> it’s already gone?",
    "404: Unexpected token &lt;page&gt; in your browser.\nTry checking the homepage or <code>git status</code> your way back to safety.",
    "404: Panic! The server tried to find this page but hit a 0xDEADBEEF.\nReload or <code>cd ~/projects</code> to recover.",
    "404: This page has wandered into an infinite loop…\nBreak out with the homepage or <code>ctrl + c</code> before it crashes your day."
  ];

  const message = messages[Math.floor(Math.random() * messages.length)];
  document.getElementById('error-message').innerHTML = message;
</script>